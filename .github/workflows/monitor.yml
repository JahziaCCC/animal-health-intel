      - name: Restore state cache
        uses: actions/cache@v4
        with:
          path: state.json
          key: animal-state-${{ github.run_id }}
          restore-keys: |
            animal-state-
